# GUIDE plan

## Goal

Explain **ainew itself**: infrastructure, architecture, every sidebar module (including Level 1), and what each menu can do. Diagrams in every architecture part. Turkish and English.

## Out of scope (this delivery)

- RAG / `docs/rag_seed` ingest
- Live VM/host inventory as the spine of the document

## Packs

| Pack id | Content |
|---------|---------|
| `full` | Entire product (1–20), install, containers, AI flow |
| `linux` | Linux module + SSH, physical hosts, Level 1 |
| `virtualization` | Virt sidebar only |
| `windows` | Windows module only |
| `openshift` | OpenShift + oVirt/OLVM + KubeVirt |

## Later

Optional short RAG cards derived from module files — not now.
