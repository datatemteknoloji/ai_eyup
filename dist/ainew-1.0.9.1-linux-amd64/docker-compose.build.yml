# Opsiyonel online derleme (internet/registry gerekir).
# Offline müşteri kurulumunda KULLANMAYIN — install-rhel.sh images/ yükler.
#
#   docker compose -f docker-compose.prod.yml -f docker-compose.build.yml build
#
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
